import React, { Component } from "react";

class EventTracker extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        {/* <button
            onClick={() =>
             setState({ totalCount: this.state.totalCount + 1 })
            }
          >
            V
          </button>
          increment
        </button> */}
      </div>
    );
  }
}

export default EventTracker;
